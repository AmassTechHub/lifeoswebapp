import { ModulePlaceholder } from "@/components/layout/ModulePlaceholder";

export default function GoalsPage() {
  return (
    <ModulePlaceholder
      title="Goals"
      description="Vision → Annual → Quarterly → Monthly → Weekly → Daily. Everything connects."
    />
  );
}
